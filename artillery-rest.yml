config:
  target: "http://localhost:9090"
  phases:
    - duration: 60
      arrivalRate: 250
  defaults:
    headers:
      Content-Type: "application/json"

scenarios:
  - name: "REST API - Create a task and use its ID"
    flow:
      - get:
          url: "/tasks"